---
trigger: always_on
---

When using icon buttons within a DataGrid, avoid relying on hover-based transition effects to convey their function or state. The button’s color or visual style must be clearly visible in its default state, without requiring user interaction. This ensures that actions are immediately identifiable and improves overall accessibility and usability of the interface.
